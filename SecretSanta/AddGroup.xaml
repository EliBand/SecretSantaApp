<Window x:Name="wnd_addGroup" x:Class="SecretSanta.AddGroup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SecretSanta"
        mc:Ignorable="d"
        Title="Gruppe Hinzufügen" Height="60" Width="300" ResizeMode="NoResize" WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <TextBox x:Name="tb_newGroup" TextWrapping="Wrap" VerticalContentAlignment="Center"/>
        <Button x:Name="btn_addGroup" Content="Hinzufügen" Grid.Column="1" Margin="0,-1,0,0" Click="btn_addGroup_Click"/>

    </Grid>
</Window>
